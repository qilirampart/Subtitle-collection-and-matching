# 全流程测试记录（2026-08-06）

## 测试素材

- 频道：`https://www.youtube.com/@Bound2Drama`
- 视频：`https://www.youtube.com/watch?v=6lVBMBl8QGw`
- 测试范围：频道 1 条、视频前 60 秒或前 180 秒

## 结果

| 流程 | 结果 | 说明 |
| --- | --- | --- |
| 频道采集 | 通过 | 成功读取 1 条视频，视频 ID `6lVBMBl8QGw`，频道 `Bound2Drama`。 |
| 独立视频下载 | 通过 | Python yt-dlp 格式筛选失败后自动回退 nightly + Node；前 60 秒生成约 2.69 MB MP4，耗时约 10 秒。 |
| 直出字幕 | 通过 | 获取 `youtube_transcript_panel` 字幕，英文，前 180 秒约 1506 字。 |
| 批量字幕准备 | 通过 | 1 个视频生成 6 个可匹配片段，0 条进入 ASR 待处理。 |
| ASR 兜底 | 通过 | 使用下载的 60 秒 MP4 走本地素材 ASR，约 5 秒返回 384 字。 |
| 封面下载 | 通过 | 封面约 22 KB，成功写入 `output/covers/6lVBMBl8QGw.jpg`。 |
| 封面检测 | 通过 | 返回 `safe`，置信度 `0.95`，无错误。 |
| 匹配登录 | 通过 | 公司内网连接恢复后可以完成登录请求。 |
| 匹配比较 | 通过 | 公司内网恢复后，用户在服务页面完成单条英文字幕验证，耗时约 `3.15 秒`，返回内容命中但剧名歧义的 `review_required`；随后桌面项目直接调用 API 也成功，1497 字字幕耗时 `2.53 秒`，返回 `matched=true`、`outcome=confirmed_match`、`title_resolution=unique`。此前 45 秒和 180 秒超时属于服务端暂时未就绪或短时负载。 |
| UI 页面与任务状态 | 通过 | 7 页面导航、独立页实例化、快照保存和恢复逻辑检查通过。 |

## 本次修复

- Python `yt-dlp` 报“Requested format is not available”时，自动使用项目内 nightly 工具回退。
- 有前段范围时优先选择渐进式 MP4，避免分离音视频流让 ffmpeg 长时间等待。
- 取消 nightly 下载时使用进程树清理，避免残留 `ffmpeg` 和 `yt-dlp` 子进程。

## 后续

- 待匹配服务恢复后，重新验证独立匹配页的提交、进度、暂停、取消和三态结果展示。
- 继续补充逐条失败重试和素材实体级关联。
