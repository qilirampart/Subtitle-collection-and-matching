# 匹配 API 接入实施记录（2026-08-05）

## API 来源

- 接入说明：`E:\点众\小说库相似度比对服务工作区\docs\87_短剧字幕匹配API接入说明_2026-08-05.md`
- 默认服务地址：`http://novel-similarity-dev.dzkjm.cn`
- 客户端不复制小说库、SQLite 索引或 Qdrant 数据，只通过 API 调用中央服务。

## 已实现

- 复用同一个 `requests.Session()` 完成登录和后续请求，支持服务端 HttpOnly Cookie 会话。
- 批量上传字幕 CSV，保留视频 ID、标题、频道、字幕来源、片段序号、YouTube 时间范围和原始字幕等溯源字段。
- 显式提交 `top_k`、`window_limit`、`semantic_enabled` 和 `semantic_window_limit` 参数。
- 轮询任务详情，并将 `partial_failed` 作为终态处理。
- 按 `result_payload_json.decision.status` 解析 `matched`、`review_required` 和 `not_matched`，不会把候选列表直接当成命中。
- 视频级聚合展示确认命中片段数、待复核片段数、未命中片段数和候选剧集。
- 接入服务端任务暂停、继续、取消接口。
- 主界面默认填入匹配服务地址，并显示服务端任务进度和失败数量。

## 待后续联调

- 验证 `partial_failed`、暂停、继续、取消在云端运行时的实际表现。
- 后续如需完整证据，可接入 `evidence-context/{window_uid}` 和服务端复核/Excel 导出接口。

## 验证结果

- 云端 `/api/v1/auth/me` 可达，未登录返回 `401 login required`。
- 云端 `/docs` 返回 `200`。
- 使用真实操作员账号完成登录，服务端返回有效用户会话。
- 使用项目已有 1 条 YouTube 字幕样本提交批量任务，任务从 `queued`、`running` 到 `completed`，完成 1 条、失败 0 条。
- 最终结果正确读取 `result_payload_json.decision`：`status=review_required`、`matched=false`，原因是 `multiple_books_with_strong_lexical_evidence`；客户端没有将候选列表误判为确认命中。
- 本地编译通过。
- 本地回归测试：`22/22` 通过。
