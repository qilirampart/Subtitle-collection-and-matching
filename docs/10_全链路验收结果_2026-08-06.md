# 全链路验收结果（2026-08-06）

## 测试范围

- 样本频道：`https://www.youtube.com/@Bound2Drama`
- 样本视频：`https://www.youtube.com/watch?v=6lVBMBl8QGw`
- 媒体与字幕范围：前 60 秒
- 验收运行目录：`output/test_runs/acceptance_20260806_160744`
- 真实服务：YouTube、已配置 ASR、已配置封面检测模型、公司内部字幕匹配服务。

## 结果

真实服务验收 `14/14` 通过；离线自动化回归 `24/24` 通过；主界面离屏实例化成功，已创建 8 个导航页面。

| 链路 | 结果 | 耗时 | 验收信息 |
| --- | --- | ---: | --- |
| 频道采集 | 通过 | 2.06s | 成功采集 2 条视频。 |
| 单条视频下载 | 通过 | 15.47s | 成功下载前 60 秒 MP4。 |
| 单条音频下载 | 通过 | 37.19s | 成功生成可转写的前 60 秒音频。 |
| 直出字幕获取 | 通过 | 1.55s | 获取 `youtube_transcript_panel` 英文字幕，共 581 字。 |
| 字幕切段 | 通过 | 0.00s | 单条视频生成 2 个可匹配片段。 |
| 字幕 Excel 导出 | 通过 | 0.03s | 导出 1 行完整字幕。 |
| 批量字幕准备 | 通过 | 3.91s | 2 条视频生成 3 个片段，无待 ASR 项。 |
| 单条封面下载 | 通过 | 0.41s | 封面文件成功保存并关联视频 ID。 |
| 单条封面检测 | 通过 | 4.48s | 返回结构化检测结果，等级为 `review`，置信度 0.85。 |
| 批量封面下载 | 通过 | 0.44s | 2 张封面均成功。 |
| 批量封面检测 | 通过 | 3.78s | 三路并发入口完成 2 张封面检测。 |
| 本地音频 ASR | 通过 | 4.28s | 识别出 389 字。 |
| ASR 兜底切段 | 通过 | 4.42s | 模拟无直出字幕状态，复用下载音频生成 1 个可匹配片段。 |
| 批量匹配 | 通过 | 6.66s | 登录、提交、轮询均完成，任务状态 `completed`，按视频聚合 2 条结果。 |

## 基础稳定性

- `TaskControl` 的暂停、继续与取消检查点已单元测试通过。
- `TaskStateStore` 的临时文件原子写入和恢复读取已单元测试通过。
- UI 离屏检查确认主窗口和 8 个独立页面能够创建，不依赖真实网络请求。
- 验收脚本不会把密码、Cookie 或 API Key 写入报告；匹配账号只从环境变量读取。

## 观察项

- 本次样本的 Python `yt-dlp` 仍未找到可用格式，但软件已自动回退到内置 nightly 工具，视频下载完成。这是已覆盖的兼容回退，不影响可用性。
- 首轮验收时，音频“直连字节范围”路线受代理出口的短时 `403` 影响而回退到分段下载，前 60 秒耗时约 37 秒。后续改为“配置代理优先，失败后直连一次”，并保留分段下载作为最终兜底；单独复测前 60 秒直接 Range 成功，输出 `1,165,185 bytes`，媒体传输耗时 `1.156 秒`、速率 `0.96 MiB/s`，总耗时约 6.23 秒。
- 同日的完整验收仍出现一次代理 `403` 加直连超时，音频安全回退后耗时 `35.94 秒`，但全流程 `14/14` 通过。因此 Range 是可用的高速路径，但会受当前代理出口与 YouTube CDN 路由抖动影响，不能作为唯一下载路径。
- 当前运行环境为 Python 3.9，依赖库出现弃用提示；本轮不影响功能，但后续打包环境建议升级到 Python 3.10 或更高版本。
